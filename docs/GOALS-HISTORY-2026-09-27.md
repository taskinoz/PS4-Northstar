# GOALS.md history, moved 2026-09-27

The handoff rows, user-testing backlog and dated checkpoints below were in [GOALS.md](GOALS.md) until 2026-09-27. They describe past states and are not current status. The earlier history is in [GOALS-HISTORY-2026-09-17.md](GOALS-HISTORY-2026-09-17.md).

## Active handoff (as of 2026-09-24)

| Goal | Work | Evidence / next test |
|---|---|---|
| G01/G07 | Source audit documented; native-registration inventory generated | Pinned PC baseline above; 62 explicit ADD_SQFUNC declarations. Builtin hooks and resource features are separately covered by G08–G25. |
| G03 | SERVER MapSpawn Before/After dispatch implemented, built and deployed on 2026-09-23 | Host tests pass; shadPS4 installs both hooks with protection=0 and completes UI startup. PRX `6c48660219ef7b358a7f4f73cb55905bed46b799aa12cb7144e9be005ce1a9ac`; log `work/stage2/iterations/20260923-140725/shad-new-lines.log`. **Updated evidence:** fresh user session and AI.Harness automatic launch verify SERVER After: SvLoadoutsMP_Init and SERVER lifecycle completed. Next test: leave/re-enter and inspect post-connection errors. |
| G02 | Schema rebased on PC 231 (2026-09-23) | `Northstar.PS4` ships `persistent_player_data_version_929.pdef` = PC 231 verbatim plus the console black market (`bm`), 56,350 data bytes against the 56,781-byte buffer; generated by `scripts/pdef/build_ps4_pdef.py` from `vendor/NorthstarMods`. The runtime generator it replaced is removed. Lobby and a hosted Kodai match run with zero script errors. Byte layout matches a PC 231 server except the appended `bm`. Open: mod `.pdiff` compilation. |
| G04/G05 | Still local only | `runtime_persistence.inl` marks fully signed-on slots READY_INSECURE, as before. No remote pdata fetch or write. |
| G14/G15 | Browser and join verified live (2026-09-23) | 93/93 live servers parsed; Atlas `auth_with_server` then connect accepted by a public server (`mp_complex3`) after `EnsureConnectUid` re-applies the imported uid the engine resets to `1`. An expired identity now tells the player how to re-export it. Open: heartbeats/advertising, required-mod downloads, staying connected (see G25 and the pipeline cache note in TECHNICAL-NOTES). |
| G06/G19 | DecodeJSON table insertion fixed and verified in the UI VM (2026-09-24) | NewSlot's bool is not success (false after every rehash), and the value reference leaked; see TECHNICAL-NOTES. Harness `json` round trip of a 20-member document (nested arrays/objects, floats, bools, escapes, `\u00e9`, duplicate key) matched except the null member, which PC also drops; a truncated document raised PC's parse-error message. PRX `3bc339ee`. Not yet exercised in CLIENT/SERVER VMs. |
| G25 | Level-transition crash fixed by shadPS4 `ca89b01` (2026-09-24) | Crashed 3/3 on `5b92da8` (also joining busy servers); 20/20 harness hops pass on `ca89b01`, which is now the documented minimum. Mod file index re-enabled on the strength of it (loads 20-35% faster). Next: repeat remote joins to populated servers and a full match -> lobby return. |

## Backlog from user testing (reported 2026-09-24, resolved by 2026-09-27)

Reported by the user after v0.2.1-alpha, on shadPS4 `ca89b01`.

**Status 2026-09-25** (details in TECHNICAL-NOTES, "Backlog from v0.2.1 user testing"):
1. Controller support added through a Northstar.PS4 override of `menu_mode_select.nut`. Second pad test: up/down did nothing (PS4 navigates only along explicit .menu nav links, which the nested mode rows lack). 2026-09-27: all pad movement done in script; verified with scripted pad input and screenshots. **Needs a confirming pad test.**
6. **Fixed 2026-09-27**: mod localisation files did not load because the PS4 `AddFile` does not expand `%language%`; the probe now passes `_english`.
7. shadPS4 `fc5d2cc2` renders matches black (HUD only). Bisected to upstream `c6fa48c7` (#5110); `2b5666b3` is the newest good build. Reported as shadps4-emu/shadPS4#5124.
2. `mp_box` cannot load: no BSP in the PS4 or PC game files. `map mp_box` still leaves the game stuck; a guard attempt hung and was removed. Open.
3. **Fixed**: native `ns_start_reauth_and_leave_to_lobby`; all three leave paths return to a `tdm` lobby in 10-11 s on the harness. Needs a remote-server check.
4. **Fixed**: native `setplaylist`/`playlist`; private match -> main menu -> multiplayer gives a normal lobby.
5. Emulator-side: shadPS4's caches, not the game's texture budget, fill VRAM (Eden peaks at 5.5 GB of 6). No game-side fix found.

Original report:

1. **Custom modes menu has no controller input** (G22). The Northstar custom game modes menu can't be navigated with a controller; make it work without a mouse.
2. **Test loading the custom map `mp_box`** (G11/G12). Shipped by Northstar.Custom; untested on PS4. Check VPK/rpak mounting and that it loads and returns.
3. **Quit to lobby stalls** (G03). Leaving a match to the lobby stalls. Profile where (server VM destroy, persistence write, level shutdown) with the harness.
4. **Quit to main menu leaves the private match loaded** (G03/G15). Private match -> main menu -> enter multiplayer again puts the player straight back into the old private match. The local listen server or its lobby state survives quitting to the main menu; it should be torn down as on PC.
5. **Busy modded server: GPU out of memory.** Joined the most-populated server (Lexi's "Aegis Attrition", `mp_drydock`, 29 server mods) and played until shadPS4 stopped with `vk::Result=ErrorOutOfDeviceMemory` (assertion on `GpuCommandProcessor`), not the old transition crash. GTX 1060 (VRAM size not confirmed; Windows reports its 4 GB cap). Log archived at `work/stage2/sessions/20260924-0219-vram-oom/session.log`. Earlier joins to the same server on `5b92da8` hit the fixed transition crash. Check whether VRAM use grows during the match (leak in the port or shadPS4's new buffer manager) or is just too high for this GPU, and compare an unmodded server on the same map. Server mods: CustomWeapons, MoussGlass mods for lexi, Prop manager, Chat logger, J0w03L.TactiRefresh, WeaponFireCallbacks, Admin Abuse, Airborne Executions, Guided Missile, Khalmee.EnhancedFlight, RCON, Spawn Mod, SwitchWepons, Translate Mod, Nyami.PlayerVote, Nyami.TitanFix, fvnknoots.ServerMod, lexi.extendreminder, ServerUtils, HoloPilot, Firestar.mod, Pilot Phase Rework, cheater, lexi.discordchatlogging, Neko's Better AI Pilots, Nekos First Person Executions Fix, plus Northstar.Client/Custom/CustomServers 1.31.13.

### First implementation checkpoint

The missing `SvLoadoutsMP_Init` callback explains why the captured SERVER path never registered `EndUpdateCachedLoadouts` or ran its default-loadout setup. `runtime_server_vm.inl` now loads enabled mods' ServerCallback entries per VM and surrounds the native CodeCallback_MapSpawn with ordered Before/After calls. Callback metadata failure stops that VM setup; missing script callback names are logged and skipped as on PC; only the engine callback determines the return value. Reset per-VM dispatch state on creation, including owner-address reuse. SERVER Destroy, InitScriptCallback, typed second-pass natives and frame-queue work remain outstanding.

Tests: `Test-NorthstarProfile.ps1` (catalog/callback ordering and failure behavior, JSON, KeyValues/live files, RPAK config, packaging), `Build-Northstar.ps1 -EnableRuntimeManifest -Output dist/northstar-parity-server`, UI startup probe, `python scripts/Update-NorthstarApiInventory.py --check`. New integration work must test the lobby, not repeat startup and call it complete.

### 2026-09-23 harness and persistence checkpoint

- Added opt-in `AI.Harness` and [local command helper](AI-HARNESS.md): automatic one-shot Northstar local launch, console commands, named menus, back, and correlated status/replies. No retail archive changes. UI thread transport, no web listener or controller injection.
- G03: the user's fresh multiplayer session **did** execute SERVER After: SvLoadoutsMP_Init and complete SERVER lifecycle. The previous missing signal/default-loadout initialization errors were absent in that session. Return/re-entry remains unverified.
- G02: runtime-generated 929 schema now appends the missing root ints `newPrimeTitans` and `netWorth`, validated against enabled PC 231 declarations. Host tests cover prefix preservation, idempotence, scope, duplicates and conflicting types; existing fields and source mods remain untouched. Full schema/pdata compatibility is still open.
- G06/G19: first harness live run found DecodeJSON failing to insert a string into a Squirrel table (`string could not be stored`). Portable parser tests do not validate native table marshalling. Harness uses bounded native field extraction instead; fix and exercise general JSON table insertion separately.

- Harness validation: runtime `370d806ec891702bd07161eaec89d78f497436b7c70c1853a40534631770114f`; automatic launch reached SERVER lifecycle completion twice, including `work/stage2/iterations/20260923-165316/shad-new-lines.log`. Correlated status replies succeeded after startup and after a console `disconnect` request. Profile regression suite and API inventory check pass.
- **Historical blockers (resolved by the repair checkpoint below):** post-connection UI `Invalid item path: "newTitanExecutions"` and SERVER `Invalid var name 'factionGiftsFixed'`. Current-session filtered evidence: `work/parity-audit/harness-live-summary.log`. Previous `newPrimeTitans`/`netWorth` failures were absent. Full lobby readiness and repeated successful map changes remain unverified. Harness continued responding after script-error recovery; one command timed out during loading and was consumed later, confirming timeouts are not cancellation.


### Active 2026-09-23 harness-driven lobby repair

G02/G01 owner: current task. Iterative launch fixes are in the native generated-schema path, not edits to upstream callers. Confirmed failures so far: missing newTitanExecutions/factionGiftsFixed; missing newCommsIcons/randomFactionLevelUnlocks; missing Monarch loadoutWeaponsAndAbilities enum entries; undersized unlockedPilotSkins. The generator now validates additional scalar/array declarations against PC, appends missing loadout/kit enum values while retaining existing indices, and increases numeric int capacities by matching field name **and struct scope**. Larger PS4 arrays and enum-sized declarations are retained. Enum and capacity changes alter binary offsets; this is not save migration or raw PC pdata compatibility. Test evidence and final live outcome will be recorded below.


### 2026-09-23 lobby repair result

- **Verified on shadPS4:** harness automatic launch reached `mp_lobby`, and two status replies 20 seconds apart reported `connected=true`, `lobby=true`, `level=mp_lobby`. The fresh session contained **zero SCRIPT ERROR entries**, including through menu opening. Startup build: `4cf10c9a765d65fae291e5487a0dfef1c84c9c781ae7bae56e41b46d526de25a`; iteration `work/stage2/iterations/20260923-171208/shad-new-lines.log`; extended evidence `work/parity-audit/lobby-fix-session5.log`, `lobby-fix-status5.json`, `lobby-fix-stable5.json`.
- Fixed observed missing root fields (Titan executions, comms icons, faction gift migration, emoji initialization and reward counters), missing Monarch weapon/ability/kit enum values, and undersized numeric persistence arrays used by current PC cosmetics. Source core mods and retail VPKs unchanged. Generated local schema is 35206 bytes from a 34140-byte base; append-only roots alone preserve the prefix, but enum growth and integer capacity expansion **change binary layout**. Existing enum indices remain stable. Save migration, PC pdata conversion, complete schema and real account persistence remain G02/G04/G05 work.
- Validation: `Test-NorthstarProfile.ps1` passes; native build passes; actual schema-pair test validates root additions, enum index retention and idempotence; host tests cover duplicate/conflicting roots, array dimensions, scoped growth, no shrink and enum-array retention. Detailed failure sequence: `work/parity-audit/lobby-fix-errors1.txt`, `lobby-fix-errors2.txt` and iteration logs.
- **Separate remaining teardown issue:** after a harness `disconnect`, shadPS4 terminated with `[Debug] <Critical> (shadPS4:GpuSchedPriorityPendingOpsRunner) ... Unhandled Exception code 0xc0000005 at 0x7ff93f72cca7`. No guest script error was logged. This establishes the reporting thread, not root cause; do not attribute it conclusively to either the mod or driver without a stack. Re-entry in the same process is not verified. The unconsumed status request was archived as `work/parity-audit/lobby-fix-unconsumed-status.json` before a fresh repeat boot.

- **Repeat confirmed:** second fresh launch `20260923-171632` also reached and remained in mp_lobby; immediate and delayed harness replies both report connected/lobby true. `work/parity-audit/lobby-fix-repeat-session.log` has zero script errors or critical entries at capture; state evidence is in `lobby-fix-repeat-status.json` and `lobby-fix-repeat-stable.json`. Emulator left running in the lobby for user inspection. This verifies repeated fresh launches, not in-process leave/re-entry.
